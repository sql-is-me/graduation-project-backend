# Tomcat
server:
  port: 9902

# Spring
spring:
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  application:
    name: user-service
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        shared-configs:
          - application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
  mail:
    host: smtp.qq.com
    port: 465
    username: 2244509212@qq.com
    password: olrxpjizyquweadh
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
          starttls:
            enable: false
  config:
    import:
      - nacos:user-service-dev.yml

wechat:
  app-id: wxec253319064722ad
  app-secret: 3364249864405979c03493f460b7e496
