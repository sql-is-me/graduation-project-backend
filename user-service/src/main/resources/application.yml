# Tomcat
server:
  port: 9902

# Spring
spring:
  application:
    name: user-service
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        shared-configs:
          - application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
  config:
    import:
      - nacos:user-service-dev.yml

# 文件服务访问基础地址
file:
  avatar-path: http://127.0.0.1:9900/pics/avatars
  tp-path: http://127.0.0.1:9900/tps
  tm-path: http://127.0.0.1:9900/tms

wechat:
  app-id: wx1234567890abcdef
  app-secret: abcdef123
